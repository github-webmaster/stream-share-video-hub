# Database Configuration
POSTGRES_USER=streamshare
POSTGRES_PASSWORD=TestPass123Min32!
POSTGRES_DB=streamshare
DATABASE_URL=postgresql://streamshare:TestPass123Min32!@db:5432/streamshare

# JWT Secret (64+ characters)
JWT_SECRET=base64example64chars==

# API Configuration
PORT=8081
NODE_ENV=production
DEBUG_ENABLED=false
LOG_LEVEL=info

# Storage Configuration
STORAGE_PATH=/data/videos
MAX_FILE_SIZE_MB=500
ALLOWED_TYPES=video/mp4,video/webm,video/quicktime,video/x-msvideo
QUOTA_MB=10240

# CORS Configuration (MUST be HTTPS for Cloudflare)
CORS_ORIGIN=https://yourdomain.com

# S3 Configuration
S3_ENDPOINT=minio:9000

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=MinioTest456Min16!
MINIO_DOMAIN=yourdomain.com

# Frontend Configuration (MUST be HTTPS for Cloudflare)
VITE_API_URL=https://yourdomain.com/api
VITE_MEDIA_URL=https://yourdomain.com/media

# Domain Configuration
DOMAIN=yourdomain.com
API_DOMAIN=api.yourdomain.com
MINIO_DOMAIN=minio.yourdomain.com

# PostgREST (Optional)
PGRST_DB_SCHEMA=public
PGRST_DB_ANON_ROLE=anon
