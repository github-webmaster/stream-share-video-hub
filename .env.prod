# Database Configuration
POSTGRES_USER=streamshare
POSTGRES_PASSWORD=TestPass123Min32!
POSTGRES_DB=streamshare
DATABASE_URL=postgresql://streamshare:TestPass123Min32!@db:5432/streamshare

# JWT Secret (64+ characters)
JWT_SECRET=base64example64chars==

# API Configuration
PORT=8081
NODE_ENV=production
DEBUG_ENABLED=false
LOG_LEVEL=info

# Storage Configuration
STORAGE_PATH=/data/videos
MAX_FILE_SIZE_MB=500
ALLOWED_TYPES=video/mp4,video/webm,video/quicktime,video/x-msvideo
QUOTA_MB=10240

# CORS Configuration
# Since we are using relative paths (Nginx proxy), the origin is the same as the site
CORS_ORIGIN=https://stream.1776.cloud

# S3 Configuration
S3_ENDPOINT=minio:9000

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=MinioTest456Min16!
MINIO_DOMAIN=minio.stream.1776.cloud

# Frontend Configuration
# Empty API URL means use relative path (e.g. /api/...) which goes through Nginx
VITE_API_URL=
VITE_MEDIA_URL=/media

# Domain Configuration
DOMAIN=stream.1776.cloud
API_DOMAIN=api.stream.1776.cloud
MINIO_DOMAIN=minio.stream.1776.cloud

# PostgREST (Optional)
PGRST_DB_SCHEMA=public
PGRST_DB_ANON_ROLE=anon
